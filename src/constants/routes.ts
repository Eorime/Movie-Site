const home = "/";
const watchlist = "/watchlist";
const movies = "/movies";
const movieDetails = "/movies/:movieID";
const series = "/series";
const seriesDetails = "/series/:seriesID";

export const routes = {
  home,
  watchlist,
  movies,
  movieDetails,
  series,
  seriesDetails,
};
